# Ruflo orchestration configuration for all applications

applications:
  - name: dotacje-landing-boss
    agent:
      type: default
      memory:
        size: 512MB
      ci_cd:
        enabled: true
  - name: prime-propertiespl
    agent:
      type: default
      memory:
        size: 512MB
      ci_cd:
        enabled: true
  - name: clawdbot
    agent:
      type: default
      memory:
        size: 512MB
      ci_cd:
        enabled: true
  - name: prime-academy-bur-assistant
    agent:
      type: default
      memory:
        size: 512MB
      ci_cd:
        enabled: true

orchestration:
  central:
    enabled: true
    settings:
      timeout: 300